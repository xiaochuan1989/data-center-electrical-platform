# 数据中心电气设计与选型平台——产品与技术文档

> 当前版本：v2.5.0
> 更新日期：2026-09-15
> 历史名称：UPS 智能选型助手
> 文档职责：说明当前产品范围、模块边界、数据来源、关键计算口径与限制。

## 1. 产品定位

平台面向销售、售前和技术工程人员，以“项目工作台 + 工具中心”为双入口，整合现有 UPS、电池能力及逐步网页化的电气设计工具。平台为 Vite 构建的纯静态应用，部署在 GitHub Pages，不依赖服务器、Docker 或账号系统。

项目、收藏、用户输入和 AI 配置仅保存在当前浏览器。页面访问码只是提示性门槛，不构成真正的权限或数据保密机制。

## 2. 当前导航与能力

| 模块 | 当前能力 | 主要来源 |
|---|---|---|
| 项目工作台 | 项目信息、新建/复制/保存、JSON 导入导出、跨模块参数复用 | 统一项目格式 |
| 工具中心 | 按业务能力展示当前有效工具，重复和历史版本不作为独立入口 | `src/platform/tool-registry.js` |
| UPS 与电池 | 智能选型、UPS 与电池配置、后备时间反算、方法一/锂电、数据中心方案校核 | v1.8.39 成熟功能、`锂电池-选型模板-A00.xlsx` |
| 产品数据库 | 105 款 UPS、关键词/收藏筛选、双表对照、列宽和上下分栏调整、型号全景及浏览器独立窗口 | 常用 UPS 速查表 V8.0 |
| 负荷与配电 | 负荷汇总、变压器初选、支路断路器、母线电流 | 数据中心负荷和母线电流工作簿 |
| 电缆选型 | 电缆计算、修正系数表、224 条电缆数据库、50 条中美线规对照 | `B-电缆选型-A00.xlsx`、GB 50217-2018 |
| 铜排计算 | 最佳规格、结构优先方案、97 条 DIN 载流量数据 | `铜排载流量-A03.xlsx`、DIN43671-1975 |
| 母线系统 | 母线电流与智能母线基础配置 | 智能母线配置与选型 |
| 电能质量 | APF、SVG 容量初步校核 | APF 容量配置、SVG+APF 交互计算 |
| 模板中心 | 只登记当前有效、经检查的模板；原始工作簿默认不公开发布 | 工具治理清单 |

“编码与交付”在 v2.4.0 暂时从侧栏、项目流程和工具中心隐藏。相关视图、导出和项目字段仍保留在代码中，便于后续恢复，不属于当前可见功能。

## 3. 技术结构

```text
index.html
  ├─ 成熟 UPS / 电池 / 产品库界面与数据
  └─ src/main.js
       ├─ src/platform/app-shell.js       平台外壳、导航、业务界面
       ├─ src/platform/project-schema.js  统一项目格式
       ├─ src/platform/project-store.js   IndexedDB 与 JSON 导入导出
       ├─ src/platform/tool-registry.js   工具中心登记
       ├─ src/modules/engineering-calculators.js  纯计算函数
       ├─ src/data/*.json                 工程基础数据
       └─ src/css/platform-v2.css         平台布局与响应式样式
```

成熟功能继续留在 `index.html`，新工程模块使用原生 JavaScript ES modules。计算函数不依赖 DOM，可由 Node 测试直接验证。页面通过 Vite 打包为 `dist/`，GitHub Pages 只发布构建产物。

## 4. 数据与持久化

| 存储 | 内容 |
|---|---|
| IndexedDB `dc_electrical_platform_db` | 项目、迁移状态 |
| IndexedDB `ups_data_db` | 用户导入的 UPS 产品数据 |
| localStorage | 当前项目 ID、收藏、界面偏好、旧版兼容数据 |
| sessionStorage `ups_auth` | 当前标签页访问状态 |

首次进入 v2 会复制旧版配置、历史和偏好到新项目的 `legacy` 区域。迁移完成前不删除旧数据，迁移成功后也保留旧键值作为回退来源。

统一项目的核心字段包括：`schemaVersion`、项目信息、拓扑、负荷、UPS、电池、开关与监控、电缆、铜排/母线、电能质量、模板和旧版兼容数据。

## 5. 电缆计算口径

电缆页面按 `B-电缆选型-A00.xlsx` 的输入和推荐逻辑实现：

- 电缆类型：BV/BVR 铜芯，YJV/YJLV/YJY/YJLY 铜芯。
- 芯数：单芯或三芯/五芯；直流及 BV/BVR 强制单芯。
- 环境温度：25、30、35、40℃。
- 并联根数：1～4；对应原表 `F4`。
- 并列根数（S=d）：1～6；对应原表 `G4`，始终允许下拉选择。
- 电源系统、单芯排列、桥架类别和叠置层数按原表条件参与修正。

原表公式仅在“直流单层”或“交流三芯/五芯单层”条件使用空气中并列修正。交流单芯等其他条件使用桥架/叠置修正。网页保留并列根数的选择，但用文字提示该值当前是否参与计算，避免把“不适用”误解成“不能填写”。

推荐流程：从 224 条基础数据中按类型、芯数、温度和排列筛选；应用修正系数；乘以并联根数；选择满足负载电流的最小截面。超过 1600A 时提示优先考虑母线系统。

## 6. 铜排计算口径

铜排页独立于电缆，使用 97 条 DIN43671-1975 数据。输入包括负载电流、安装环境、表面处理和温升标准。

```text
折算需求电流 = 负载电流 ÷ 综合系数 K_Total
负载率 = 折算需求电流 ÷ 选定规格载流量
```

输出同时给出：最佳规格、主母线结构优先方案、额定载流、总截面、负载率、PE 截面和大电流复核提示。

4000A、通风、光裸/全镀锡、IEC 50K 示例的最佳规格为 `4 × 100 × 5（四拼）`；结构优先方案为 `3 × 100 × 10（三拼）`。

## 7. UPS、电池与监控关键规则

- “计算当前配置”允许 UPS 型号留空，按手动 UPS 容量或负载功率计算；“加入项目”仍要求完整型号。
- 电池开关电流可选择按 `UPS容量 × 功率因数` 或按负载功率计算，界面以“影响成本”标识提醒。
- 监控主机/显示屏数量保留按总电池组数每 6 组 1 套，并增加按 UPS 台数每台 1 套。
- 50kVA 功率模块带中性线；100kVA 功率模块不带中性线。
- 组合式 UPS 的产品编码按机柜和功率模块分行解释；`01021107` 为 50kVA 功率模块编码。

锂电池页面自 v2.5.0 起逐项对照 `工具模板/锂电池-选型模板-A00.xlsx` 的“锂电池选型”工作表第 2 行：

```text
C2 = L2 ÷ 3.2
F2 = IF(B2 > 100, 0.95, 0.93)
I2 = 1 ÷ H2
J2 = IFS(I2≤1, 3.15, 1<I2≤3, 3.12, 4≤I2≤6, 3.05, I2>6, 3.02)
K2 = IF(A2为空,
        B2 × E2 × 1000 × H2 ÷ (F2 × C2 × D2 × G2 × J2),
        A2 × 1000 × H2 ÷ (F2 × C2 × D2 × G2 × J2))
```

其中 A2 为负载功率、B2 为 UPS 功率、D2 为电池组数量、E2 为功率因数、G2 为电池输出效率、H2 为放电时间、L2 为标称电压。K2 表示每组所需计算容量；网页同时显示原始计算值和 Excel 的整数显示值。原表 J2 未覆盖 3C～4C，网页保持该边界并提示人工确认，不擅自插值。

电池单体功率：

```text
负载功率 = 已填负载功率 × 1000
           否则 UPS容量 × 功率因数 × 1000

单体所需功率 = 负载功率
             ÷ (每组节数 × 单格数 × 逆变效率 × 每台UPS电池组数)
```

开关箱计算依据由页面下拉决定，并在结果与项目汇总中记录所选口径。

## 8. 产品数据库交互

- 产品信息和技术规格分成上下两个独立滚动区，中间分隔条可上下拖动，双击恢复均分。
- 表头列边界可调整列宽；Shift+拖动或中键拖动用于横向查看，普通拖动可选择文字。
- 单击型号打开可拖动、可缩放的非模态全景卡片；“独立窗口”可拖到另一台显示器。
- v2.4.0 数据库卡片占满当前浏览器剩余高度，不再产生大块无效空白；两个表格均可滚动至最后一行。

## 9. AI 边界

AI 可读取当前项目参数，协助检查、推荐和生成说明；合法 UPS 型号必须经过本地产品数据过滤。AI 结果不能替代厂家选型、保护配合、短路校核、施工图或注册工程师审核。

## 10. 公开构建与安全边界

- `工具模板/` 是核对来源，不进入 `dist/`。
- 新增 `src/data/` 只存放工程基础数据，不存放供应商价。
- 商务成本只允许空白价格格式，不内置供应商价或历史成交价。
- 页面访问码、价格查看码和前端混淆均不是真正安全措施。
- 发布前运行构建审计，检查模板、价格关键词和非允许文件。

## 11. 验收要求

1. 同一输入下，网页计算结果与当前有效 Excel 在显示精度内一致。
2. 空值、零值、边界档位、下拉组合和超限场景均有测试。
3. 旧浏览器数据迁移、项目导入导出和刷新恢复可用。
4. 1366×768、1920×1080 和平板宽度完成真实浏览器回归。
5. 产品数据库两个滚动区能到达最后一行，控制台无 error。
6. 公开构建不包含 `工具模板/` 或未经批准的原始工作簿。

版本历史以 `README.md` 和 Git 提交记录为准；v1.8.39 标签是平台化升级前的回退基线。
