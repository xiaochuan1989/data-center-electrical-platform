# 数据中心电气设计与选型平台

**版本**：v2.6.4 · **线上地址**：https://xiaochuan1989.github.io/data-center-electrical-platform/

面向销售、售前和技术工程人员的静态工程工具平台。采用“项目工作台 + 工具中心”双入口，把项目参数沿负荷、UPS与电池、配电、电缆、铜排、母线和电能质量贯通。“编码与交付”目前按业务决定暂时隐藏，底层代码保留，后续确认后再开放。

平台继续使用免费的 GitHub Pages，不建设服务器、Docker 或账号系统。项目数据、用户填写价格和 AI 配置只保存在本机浏览器；现有访问密码仅作为提示性门槛，不代表真正的数据保密能力。

## v2.6.4 铜排热模型复核

- 新亮全镀锡铜排默认发射率由 `0.06` 调整为 `0.05`，新增 `0.03` 电镀铜保守校核；`0.35` 仅作为原 Excel 历史参数保留并突出风险。
- 20℃铜电阻率默认值调整为 `1.7241×10⁻⁸Ω·m`；明确 `h=5W/(m²·K)` 只是自然对流初算值。
- 100%设计系数改称“热平衡极限”，不再显示为建议持续工作电流，并增加无裕量警告。
- DIN 对照改用热平衡值，避免差异随人为设计裕量变化；按电流选型不再把热缩套管等同于 A03 的涂层载流量列。
- 页面补充单片、非并联、四面散热等适用边界，以及柜壁遮挡、并排互热、接头和交流附加损耗限制。

## v2.6.3 界面修正

- 用户界面不再把工作簿版本号“A03”当作温升口径名称。
- 下拉选项改为“50K 温升修正（通风 × 1.3）”和“30K 温升基准（DIN 原值）”；A03 仅保留在计算来源说明和开发资料中。

## v2.6.2 修正

- 分开显示 DIN 查表温升、铜排相对外部环境的工程控制温升和铜排对柜内空气的有效散热温差，避免将三个不同量混为一谈。
- 规格反算默认按 `35℃ + 70K = 105℃` 自动计算铜排最高温度；密闭柜体若采用 `15K` 柜内空气温升，则局部散热温差为 `55K`，开放空气可将柜内温升设为 `0K`。
- “IEC增强 50K”改为“A03 修正口径 50K”，明确它是原工作簿的选型修正规则，不冒充 GB/T 7251 的统一母线温升限值。

## v2.6.1 修正

- 铜排规格反算的表面发射率改为可选：Excel 原始默认 `0.35`、新亮镀锡参考 `0.06`、自定义。
- 不再把原表未注明来源的 `0.35` 直接解释成镀锡；计算详情会显示本次选择和实际采用的发射率。

## v2.6.0 优化

- 铜排页拆分为“按电流选铜排”和“按规格算载流量”两个独立标签，原 A03/DIN 选型逻辑完整保留。
- 规格反算按 `铜排载流量工程计算器-A00.xlsx` 的稳态热平衡公式实现，默认 `120 × 10mm` 示例得到热平衡电流 `2512.86A`、80%建议持续电流 `2010.29A`。
- 补齐内部环境温升、最高温度、对流换热、表面发射率、电阻率、温度系数、设计裕量及显式交流电阻修正系数；结果展示每一步散热、电阻和损耗。
- 增加同规格 DIN 裸排/涂层载流量交叉对照、输入边界校验、交流附加损耗提示及大电流 Icw 专项校核提示。
- 标准说明更新为 GB/T 7251.1-2023、GB/T 24276-2025；热平衡结果定位为工程估算，不替代成套温升试验。

## v2.5.0 优化

- 锂电池计算逐项对照 `锂电池-选型模板-A00.xlsx`：标称电压反算 3.2V 电芯串联数，逆变器效率按 UPS 容量自动取值，倍率与平台电压严格按原 IFS 条件执行。
- 补齐负载功率优先/UPS 容量回退、电池组数量下拉、自动参数预览、整数显示容量和未取整精确值。
- 页面常驻展示计算方法、Excel 单元格对应关系和本次代入过程；原表未定义的 3C～4C 区间明确拦截提示。

## v2.4.0 优化

- 对照 `B-电缆选型-A00.xlsx` 恢复并列根数 1～6 下拉；所有条件均可选择，不适用时明确提示“当前不采用并列系数”。
- 产品数据库改为占满浏览器剩余可视区域，产品信息和技术规格各自完整纵向滚动，移除页面底部无效空白。
- 暂时隐藏侧栏、项目流程和工具中心中的“编码与交付”入口；相关实现未删除。
- README、产品技术文档、工程开发手册、架构说明和工具治理清单同步到 v2.4.0。

## v2.3.0 优化

- 电缆页面增加“电缆修正系数数据表”“电缆数据库”“中美线规对照表”三个独立标签，便于计算时现场查阅和对照。
- 修正系数完整展示 GB 50217-2018 表 D.0.5、D.0.6；电缆数据库 224 条、线规对照表 50 条均支持搜索、筛选和横纵向滚动。
- 基础载流量与计算后的修正结果明确分开，避免把动态计算结果误认为固定数据。

## v2.2.0 优化

- “电缆与导体”改为独立“电缆选型”，移除导体查询入口。
- 电缆输入项、224 条载流量数据、修正系数和推荐逻辑统一以 `B-电缆选型-A00.xlsx` 为准。
- 补齐芯数、电源系统、单芯排列、并联/并列根数、桥架类别及叠置层数。
- 增加中美线规查询，共 50 条 AWG/CWG 对照数据。

## v2.1.2 优化

- 铜排结果补充“最佳规格”，严格按 A03 的全表最接近满足值计算；同时保留“主母线结构优先”方案用于对照。
- 4000A 示例输出最佳规格 `4 x 100 x 5（四拼）`，结构优先方案 `3 x 100 x 10（三拼）`，额定载流、截面、负载率和 PE 截面均跟随最佳规格。

## v2.1.1 优化

- GitHub 仓库更名为 `data-center-electrical-platform`，线上地址同步更新。
- 左侧导航顶部新增明确的“收起导航 / 展开导航”按钮；桌面端收起后保留图标栏，移动端继续使用顶栏入口。

## v2.1.0 新增

- 铜排计算拆分为独立页面，完整复现 `铜排载流量-A03.xlsx` 的环境、表面处理、温升系数、结构优先级、负载率与 PE 截面规则。
- 内置 97 条 DIN43671-1975 铜排载流量数据，可按结构形式和关键词筛选，并支持表格横向、纵向滚动。
- 桌面端左侧导航可折叠为图标栏，折叠偏好保存在当前浏览器；移动端保持抽屉式导航。

## v2.0.0 交付范围

- 正式更名为“数据中心电气设计与选型平台”，保留 iTeaQ 艾特网能品牌标识。
- 新增左侧业务导航、项目工作台、工具中心和右侧全局 AI 辅助面板。
- 原样承接现有 UPS 智能选型、UPS 与电池配置、后备时间反算、电池方法一/锂电、数据中心校核及产品数据库。
- 新增模块化工程计算：负荷汇总、变压器初选、支路断路器、母线电流、电缆/导体/铜排查询、智能母线、APF 和 SVG 校核。
- IndexedDB 保存统一项目数据，支持新建、保存、复制、JSON 导入和 JSON 导出。
- 首次打开新版时复制迁移旧版 UPS 配置、历史、收藏和界面偏好；迁移不会删除旧数据，旧产品数据库继续保留。
- 标准 Excel 输出包含项目汇总、负荷计算和商务成本空白表。平台不内置目录价、供应商价或历史成交价。
- 原始 `工具模板/` 只作为公式和版本核对来源，不进入公开构建；模板必须通过宏、隐藏表、外部链接、定义名称和价格残留检查后才可发布。

## 开发与验证

```powershell
npm install
npm run dev
npm test
npm run build
```

完整旧版回归仍使用 Python 测试入口：

```powershell
python -m pip install -r requirements-dev.txt
python dev_scripts/test.py --quick
python dev_scripts/test.py --all
```

如果 Windows 的 `py` 启动器提示 `No installed Python found`，表示启动器未登记解释器，并不一定代表电脑没有 Python。可使用实际 `python.exe` 路径或 Codex 工作区提供的 Python 运行时。

## 目录结构

- `index.html`：保留成熟 UPS/电池功能和内置产品数据，并加载 v2 模块入口。
- `src/main.js`：Vite 应用入口。
- `src/platform/`：平台外壳、项目格式、IndexedDB 存储、工具注册表。
- `src/modules/`：与界面分离的纯计算函数。
- `src/data/`：从当前有效工具提取的工程基础数据，包括 97 条铜排、224 条电缆及 50 条线规数据。
- `src/css/platform-v2.css`：新版平台布局和响应式样式。
- `dev_scripts/extract_engineering_catalogs.py`：从 Excel 核对源重建非价格基础数据。
- `dev_scripts/build_tool_inventory.mjs`：重建 70 个来源文件的治理主清单。
- `docs/tool-inventory.md`：工具主清单、迁移优先级和首批验收样例。
- `docs/v2-architecture.md`：v2 架构、数据和发布边界。
- `工具模板/`：本地核对来源，不直接发布。

## 发布

GitHub Actions 先运行 Python/Node 质量检查，再执行 `npm ci && npm run build`，仅将 `dist/` 发布到 GitHub Pages。Vite 使用相对资源路径，适配当前仓库子路径部署。

v1.8.39 已建立 Git 标签，作为升级前回退基线。

## 开发文档

- [UPS选型助手_开发文档.md](UPS选型助手_开发文档.md)：当前平台产品能力、模块、数据和关键计算口径。
- [UPS选型助手_开发说明.md](UPS选型助手_开发说明.md)：当前工程目录、开发流程、测试、浏览器回归和发布说明。
- [docs/v2-architecture.md](docs/v2-architecture.md)：平台架构、数据存储和公开构建边界。
- [docs/tool-inventory.md](docs/tool-inventory.md)：工具主清单、当前核对版、迁移状态及暂缓项。
