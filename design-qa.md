# Design QA — v2.6.0 铜排双向计算

## 核对证据

- 公式核对源：`工具模板/铜排载流量工程计算器-A00.xlsx`
- 数据核对源：`工具模板/铜排载流量-A03.xlsx`、`src/data/busbar-catalog.json`
- 浏览器：Codex in-app Chromium，本地 Vite `http://localhost:4173/?v=2.6.0`
- 可见状态：进入“铜排计算”，分别检查“按电流选铜排”和“按规格算载流量”；规格反算完成默认值、交流修正和无效温度三组交互。

## 页面与交互

- 原有 A03/DIN 按电流选规格页面保持为默认标签，输入和结果链路未被覆盖。
- 新增规格反算标签，形成“来源与适用范围 → 基本输入 → 高级热工参数 → 计算方法 → 结果与 DIN 对照”的完整信息结构。
- 桌面视口中四列输入、四个主结果及过程卡片对齐，没有横向溢出；1180px 以下切换两列，900px 以下切换单列。
- 交流模式启用交流电阻修正系数；直流模式自动回到 1.00 并禁用该输入。
- 表面参数使用 Excel 默认模式时固定发射率 0.35；切换自定义模式后才允许修改，避免无意偏离原表。

## 计算证据

- 默认 `120 × 10mm`、最高温度 105℃、房间 35℃、内部温升 15K、`h=5`、`ε=0.35`：热平衡估算 `2512.86A`，80%建议持续电流 `2010.29A`，与 A00 工作簿一致。
- 页面显示值：建议电流 `2010A`、热平衡电流 `2513A`、估算工作温度 `84.3℃`、电流密度 `1.68A/mm²`。
- DIN 同规格裸排对照：`120 × 10` 单片 `1740A`，模型建议值高 15.5%；页面明确提示两种条件不同、不可互相替代。
- 交流电阻修正系数改为 4 后，热平衡电流约减半至 `1256A`、建议值 `1005A`，符合 `I ∝ 1/√R`。
- 将允许最高温度设为内部环境温度 50℃时，页面阻止计算并提示“必须高于房间温度与内部温升之和”。

## 风险控制

- 页面明确限定单片、非并联矩形铜排的自然对流稳态估算。
- 对内部温升、换热系数、发射率、设计裕量和交流电阻系数均标明其工程假设性质。
- 并排互热、相间影响、接头损耗、集肤/邻近效应、谐波、连接件和 Icw 保留专项校核提示。
- 结果称为“热平衡估算载流量”和“建议持续工作电流”，不再使用“极限安全载流量”等过强表述。

## 自动验证

- Node 计算与入口测试：通过。
- Vite 构建及公开内容审计：通过；构建中没有原始工作簿、PDF 或内置价格值。
- Python 全量回归：13/13 通过。

final result: passed
